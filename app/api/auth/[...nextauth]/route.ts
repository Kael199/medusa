// Auth.js v5 route handlers. Next 16 App Router requires GET + POST to be
// exported as standalone functions; Auth.js' `handlers` object provides them.

import { handlers } from "@/lib/auth/config";

export const { GET, POST } = handlers;
